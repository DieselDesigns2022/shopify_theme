<section class="main-404 page-width">
  <div class="main-404__inner">
    <p class="main-404__code">404</p>
    <h1>{{ section.settings.heading | escape }}</h1>
    {% if section.settings.text != blank %}<div>{{ section.settings.text }}</div>{% endif %}
    <a class="button" href="{{ routes.root_url }}">{{ section.settings.button_label | escape }}</a>
    {% if section.settings.show_search_link %}<a class="main-404__search" href="{{ routes.search_url }}">{{ 'search.search' | t }}</a>{% endif %}
  </div>
</section>
{% schema %}
{
  "name": "404 page",
  "settings": [
    { "type": "text", "id": "heading", "label": "Heading", "default": "Page not found" },
    { "type": "richtext", "id": "text", "label": "Text", "default": "<p>The page you were looking for does not exist. Return home or search the store.</p>" },
    { "type": "text", "id": "button_label", "label": "Button label", "default": "Return home" },
    { "type": "checkbox", "id": "show_search_link", "label": "Show search link", "default": true }
  ]
}
{% endschema %}
